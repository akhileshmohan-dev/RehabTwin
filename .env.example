# Local/offline default: SQLite
REHABTWIN_DB_PATH=data/rehabtwin_thread.db

# Later, for PostgreSQL/Supabase or another PostgreSQL server, replace the line above with:
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/rehabtwin
