/** Component-local aliases for the replay data shapes. */
export type {
  SessionFrame as ReplayFrame,
  SessionFrameLandmark,
  SessionFramesResponse as ReplaySession,
} from "@/types/rehab";

export type Side = "left" | "right";
